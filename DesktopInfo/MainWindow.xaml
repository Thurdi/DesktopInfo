<Window x:Name="frame" x:Class="DesktopInfo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopInfo"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:commands="clr-namespace:DesktopInfo.Commands"
        mc:Ignorable="d"
        Title="MainWindow" Foreground="{x:Null}" Background="{x:Null}" Visibility="Visible" ShowInTaskbar="False" ResizeMode="NoResize" WindowStyle="None" AllowsTransparency="True" Width="500" Height="200">
    <Grid x:Name="dataGrid">
        <Label x:Name="hostnameLabel" Content="hostname" HorizontalAlignment="Left" Margin="10,0,-310,0" VerticalAlignment="Top" Foreground="White" Grid.RowSpan="2"/>
        <Label x:Name="ipLabel" Content="ip addr" HorizontalAlignment="Left" Margin="10,20,0,0" VerticalAlignment="Top" Foreground="White" Grid.RowSpan="2"/>
        <Label x:Name="biosLabel" Content="bios ver" HorizontalAlignment="Left" Margin="10,40,0,0" VerticalAlignment="Top" Foreground="White" Grid.RowSpan="2"/>
        <Label x:Name="osLabel" Content="OS ver" HorizontalAlignment="Left" Margin="10,60,0,0" VerticalAlignment="Top" Foreground="White" Grid.RowSpan="2"/>
        <Label x:Name="uptimeLabel" Content="uptime" HorizontalAlignment="Left" Margin="10,80,0,0" VerticalAlignment="Top" Foreground="White" Grid.RowSpan="2"/>
        <tb:TaskbarIcon x:Name="MyNotifyIcon" IconSource="/icons/circle.ico" ToolTipText="Desktop Info">
            <!-- Set a simple context menu  -->
            <!-- the data context of the context menu is the NotifyIcon itself (see more about this in DataBinding samples) -->
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="About" Command="{commands:About}" CommandParameter="{Binding}">
                        <MenuItem.Icon>
                            <Image Width="16"
                                   Height="16"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Exit" Command="{commands:Exit}" CommandParameter="{Binding}">
                        <MenuItem.Icon>
                            <Image Width="16"
                                   Height="16"/>
                        </MenuItem.Icon>
                    </MenuItem>

                    <!--<Separator />-->
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Grid>
</Window>
